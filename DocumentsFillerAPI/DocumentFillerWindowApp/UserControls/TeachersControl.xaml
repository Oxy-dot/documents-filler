<UserControl x:Class="DocumentFillerWindowApp.UserControls.TeachersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DocumentFillerWindowApp.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--DisplayMemberBinding="{Binding FirstName}"-->
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Margin="10" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="{materialDesign:PackIcon Kind=ContentSave}" Margin="20, 0" Visibility="{Binding SaveChangesShowButton, UpdateSourceTrigger=PropertyChanged}" Click="Button_Click_1"/>
            <Button Grid.Column="2" Margin="10, 2" Click="Button_Click">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Add" Margin="0, 0, 10, 0"/>
                    <TextBlock Text="Удалить"/>
                </StackPanel>
            </Button>
        </Grid>
        <DataGrid
            x:Name="MainGrid"
            CanUserAddRows="True"
            ItemsSource="{Binding Teachers}"
            SelectionMode="Extended"
            SelectionUnit="FullRow"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.CanContentScroll="False"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling"
            EnableRowVirtualization="True"
            EnableColumnVirtualization="True"
            AutoGenerateColumns="False"
            HeadersVisibility="Column"
            GridLinesVisibility="None"
            RowHeaderWidth="0"
            AlternatingRowBackground="Transparent"
            BeginningEdit="MainGrid_BeginningEdit"
            PreparingCellForEdit="MainGrid_PreparingCellForEdit"
            CellEditEnding="MainGrid_CellEditEnding"
            Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding SecondName, UpdateSourceTrigger=LostFocus}" Header="Фамилия" />
                <DataGridTextColumn Binding="{Binding FirstName, UpdateSourceTrigger=LostFocus}" Header="Имя" />
                <DataGridTextColumn Binding="{Binding Patronymic, UpdateSourceTrigger=LostFocus}" Header="Отчество" />
                <DataGridTemplateColumn Header="Должность">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding AcademicTitle.Name}" VerticalAlignment="Center" Margin="5,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox SelectedItem="{Binding AcademicTitle, UpdateSourceTrigger=LostFocus}" 
                                      DisplayMemberPath="Name"
                                      ItemsSource="{Binding DataContext.AcademicTitles, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                      PreviewKeyDown="ComboBox_PreviewKeyDown"
                                      LostFocus="ComboBox_LostFocus"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
